Accordion11 from ... Use via `window.AshokaDS.Accordion11` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<NextAuthProvider>` (full provider chain in README.md — components read theme/i18n from that context).

## Related

`Accordion11Content`, `Accordion11Item`, `Accordion11Trigger`
