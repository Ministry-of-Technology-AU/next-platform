MorphingDialogTitle from ... Use via `window.AshokaDS.MorphingDialogTitle` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<NextAuthProvider>` (full provider chain in README.md — components read theme/i18n from that context).
