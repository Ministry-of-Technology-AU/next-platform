import * as React from 'react';

/**
 * TooltipProvider — from ..@0.1.0.
 */
export interface TooltipProviderProps {
  [key: string]: unknown;
}

export declare const TooltipProvider: React.ComponentType<TooltipProviderProps>;
