DropdownMenuContent from ... Use via `window.AshokaDS.DropdownMenuContent` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<NextAuthProvider>` (full provider chain in README.md — components read theme/i18n from that context).
