Accordion11Content from ... Use via `window.AshokaDS.Accordion11Content` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<NextAuthProvider>` (full provider chain in README.md — components read theme/i18n from that context).
