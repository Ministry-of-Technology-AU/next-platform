const API_KEY = process.env.CALENDAR_API_KEY || null;


function getEvents(calId:string, startTime:string, endTime:string, eventId:string){

}
//TODO: for both calID, fetch default calendar ID from .env
interface Event {
    //TODO: Add this
}
function addEvents(calId:string, event: Event){

}
